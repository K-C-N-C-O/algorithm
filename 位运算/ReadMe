2595   0x5555作为位掩码通过位与提取奇数位和偶数位
231   「二进制表示中最低位」相关的位运算技巧
       n & (n - 1) 其中 &表示按位与运算。该位运算技巧可以直接将 n的二进制表示的最低位1移除
       如果 n是正整数并且 n & (n - 1) = 0，那么 n就是 2的幂
       n & (-n) 其中 −n是 n的相反数，是一个负数。该位运算技巧可以直接获取 n二进制表示的最低位的 1。
       由于负数是按照补码规则在计算机中存储的，−n的二进制表示为 n的二进制表示的每一位取反再加上 1
       如果 n是正整数并且 n & (-n) = n，那么 n就是 2的幂
1009 如果n二进制表示的最高位的1是第i位，那么一定有2（i）<=n<2(i+1),取反按位与1求异或
461  n&(n-1) 删去最低位1，可用来位计数计算1的个数
1657  用长为26的数组统计字母出现次数，同时用位运算统计字母是否出现，最后比较两个mask是否相等，比hashset方便多了。

     

