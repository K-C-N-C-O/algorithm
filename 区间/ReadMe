分组循环模板
这个写法的好处是，无需特判nums是否为空，也无需在循环结束后，再补上处理最后一段区间的逻辑。
适用场景：按照题目要求，数组会被分割成若干段，且每一段的判断/处理逻辑是一样的。
注：虽然代码写的是一个二重循环，但 i += 1 这句话至多执行 n 次，所以总的时间复杂度仍然是 O(n) 的

i, n = 0, len(nums)
while i < n:
    start = i
    while i < n-1 and i   i+1...:
        i += 1
    (对这一段根据题目要求进行处理）
    i++;  //下一段从i+1开始
    //从 start 到 i 是一段

