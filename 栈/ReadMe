2390
从遍历的顺序来看，也就是最后遍历到了先删除，「后入先出LIFO」可以用栈来方便解决。
735
用alive标记进入元素状态
71
可以用双端队列模拟栈，优点是可以弹出最底部元素而不是只能弹出顶端元素
155
通过辅助栈可以知道一组动态数据中最小/大的那个数，因为某一时刻如果栈顶元素是a,那么可以确定栈里元素一定是在a之前进栈的，那么通过每次a进栈时把当前的最值存入栈中，
如果栈顶元素是a，一定有辅助栈的栈顶元素是最值
