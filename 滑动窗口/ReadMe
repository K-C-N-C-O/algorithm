2090 在 avgs[k] = (int) (sum / (2 * k + 1)); 这一行代码中，将整个除法表达式 (sum / (2 * k + 1)) 用括号括起来，然后再将结果强制转换为 int 类型。这种写法可以确保除法操作发生在强制类型转换之前，这样可以避免精度丢失。
     这是因为在 Java 中，如果除法操作发生在强制类型转换之前，结果会保留小数部分。然后再将带有小数部分的结果强制转换为整数类型，小数部分会被截断，这样可能会导致结果不准确。
1456 2379 记录完整变化的变量应放在循环外面
2134 数组拼接解决环形数组
