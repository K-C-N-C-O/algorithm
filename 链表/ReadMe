链表中删除一个节点就是用该节点上一个节点next直接指向该节点下一个节点
pre.next=cur.next,删除cur
链表中插入一个节点就是先找到插入位置的前一个节点，将插入节点指向插入位置后面的节点，再将前一个节点指向插入节点,因为需要修改前一个节点下一个节点，所以需要先连接上下一个节点
cur.next=pre.next;
pre.next=cur;

2
设置dummmy可以省去判断head是否为空，如果要遍历链表，要设置一个指针遍历
